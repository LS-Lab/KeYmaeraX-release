ProgramVariables.
  R a.
  R b.
  R c.
End.
Problem.
    \forall a ( 
    \forall b (
    \forall c ( 
      (a >= 0 & b >= 0 & c >= 0) 
      -> 
      (\exists a (
       \exists b ( 
        (a >= 0 & b >= 0)
      )))
    )))
End.
